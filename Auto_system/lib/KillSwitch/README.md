# KillSwitch Library

## 文件夹作用
用于监控机器人的紧急停止状态。通常连接到一个物理急停按钮或拨动开关。

## 主要函数
- `begin()`: 配置引脚为 `INPUT_PULLUP` 模式。
- `isKilled()`: 检查是否触发了急停（检测到低电平）。一旦触发，状态会锁定为 `true`。
- `reset()`: 手动清除急停锁定状态。
- `kill()`: 软件触发急停。

## 工作原理
当连接到 `PIN_KILL_SWITCH` 的按钮被按下时，引脚接地（LOW），程序将记录急停状态并停止电机。
